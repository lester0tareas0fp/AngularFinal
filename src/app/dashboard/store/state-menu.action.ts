import { createAction } from '@ngrx/store';

export const activateMenu = createAction (
    '[AM Component] Activate Menu'
);

export const hideMenu = createAction (
    '[AM Component] Hide Menu'
);

